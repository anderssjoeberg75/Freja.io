# Weather Skill

## Purpose
This skill registers the `get_weather` tool for Gemini tool-calling.

## Environment
- Uses existing Freja settings for coordinates and weather provider behavior.
- No additional environment variables are required beyond current weather configuration.
